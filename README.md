# CLAUDE.md Expense Demo

Static offline demo for showing how different `CLAUDE.md` definitions change an generated expense-management UI.

Open `index.html` in a browser. No build step, dependencies, API calls, or network access are required.
